
# Set prefix key (Ctrl+W)
set -g prefix C-w
# Unbind default prefix (Ctrl+B)
unbind-key C-b
# Send prefix to nested session (Ctrl+W)
bind-key C-w send-prefix

# Switch to last active window (Prefix Ctrl+W)
bind-key C-w last-window

# Don't wait for escape codes, just GO
set -sg escape-time 0

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
# Stay in tmux when a session is destroyed
set -g detach-on-destroy off

# Renumber windows when one is closed
set -g renumber-windows on
# NeoVim said to do it
set -g focus-events on

# Setup terminal and colors
set -g default-terminal "tmux-256color"
set -as terminal-features ",xterm-256color:RGB"

# VI keys for tmux copy mode
set -g status-keys vi
set-window-option -g mode-keys vi

#######################################################
# Key Bindings
#######################################################

# Begin selection in copy mode (v)
bind-key -T copy-mode-vi 'v' send -X begin-selection
# Toggle rectangle selection in copy mode (V)
bind-key -T copy-mode-vi 'V' send -X rectangle-toggle
# Copy selection and exit copy mode (y)
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# Set Window Titles
set -g set-titles off

# Reload tmux config (prefix + r)
bind r source-file ~/.config/tmux/tmux.conf \; display "Config reloaded!"

# Focus pane (prefix + h/j/k/l)
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Resize pane (prefix + H/J/K/L)
bind H resize-pane -L 4
bind J resize-pane -D 4
bind K resize-pane -U 4
bind L resize-pane -R 4

# Split pane horizontally (prefix + /)
bind / split-window -v -c "#{pane_current_path}"
# Split pane vertically (prefix + \)
bind \\ split-window -h -c "#{pane_current_path}"

# Cycle to previous layout (prefix + ,)
bind , previous-layout
# Cycle to next layout (prefix + .)
bind . next-layout

# Kill current pane (prefix + q)
bind q kill-pane

# Enter copy mode (prefix + g)
bind g copy-mode
# Paste buffer (prefix + v)
bind v paste-buffer

# Toggle pane synchronization (prefix + a)
bind a setw synchronize-panes

# New window in current path (prefix + n)
bind n new-window -c "#{pane_current_path}"

# Previous window (prefix + u)
bind u previous-window
# Next window (prefix + i)
bind i next-window

# Swap window left (prefix + y)
bind y swap-window -t -1 \; previous-window
# Swap window right (prefix + o)
bind o swap-window -t +1 \; next-window

# Select session (prefix + t)
bind t display-popup -E -w 40% "sesh connect \"$(
 sesh list -i | gum filter --limit 1 --no-sort --fuzzy --placeholder 'Pick a sesh' --height 50 --prompt='⚡'
)\""
# Rename window (prefix + T)
bind T command-prompt -I "#W" "rename-window '%%'"
# Rename session (prefix + S)
bind S command-prompt -I "#S" "rename-session '%%'"
# Set pane title (prefix + P)
bind P command-prompt -p "New title:" 'select-pane -T "%%"'

# Select window 1-5 quick keys (prefix + z/x/c/b)
bind z select-window -t 1
bind x select-window -t 2
bind c select-window -t 3
# NOTE: M-v was bound to both paste-buffer and select-window -t 4.
# Now v = paste-buffer, no quick key for window 4 (use prefix + 4 instead).
bind b select-window -t 5

# Select window by number (prefix + 1-9, 0)
bind 1 select-window -t 1
bind 2 select-window -t 2
bind 3 select-window -t 3
bind 4 select-window -t 4
bind 5 select-window -t 5
bind 6 select-window -t 6
bind 7 select-window -t 7
bind 8 select-window -t 8
bind 9 select-window -t 9
bind 0 select-window -t 10


# Show pane info at top of each pane
set -g pane-border-status top
# Display pane index and title in border
set -g pane-border-format '#{pane_index} "#{pane_title}"'

# Catppuccin color scheme variant
set -g @catppuccin_flavour 'mocha'

# Increase scrollback buffers
set -g history-limit 10000

# Mouse settings
setw -g mouse on


# use a theme
set -g @tmux2k-theme 'catppuccin'
set -g @tmux2k-icons-only false

set -g @tmux2k-black '#1e1e2e'
set -g @tmux2k-gray '#45475a'
set -g @tmux2k-white '#cdd6f4'
set -g @tmux2k-light-blue '#89dceb'
set -g @tmux2k-blue '#89b4fa'
set -g @tmux2k-dark-blue '#526c96'
set -g @tmux2k-light-green '#94e2d5'
set -g @tmux2k-green '#a6e3a1'
set -g @tmux2k-dark-green '#638861'
set -g @tmux2k-light-orange '#fab387'
set -g @tmux2k-yellow '#fab387'
set -g @tmux2k-dark-orange '#966b51'
set -g @tmux2k-light-pink '#f5e0dc'
set -g @tmux2k-light-purple '#cba6f7'
set -g @tmux2k-dark-pink '#93748b'
set -g @tmux2k-purple '#b4befe'
set -g @tmux2k-dark-purple '#6c7298'
set -g @tmux2k-light-red '#eba0ac'
set -g @tmux2k-red '#f38ba8'
set -g @tmux2k-dark-red '#925365'
set -g @tmux2k-light-yellow '#f9e2af'
set -g @tmux2k-dark-yellow '#958869'

set -g @tmux2k-left-plugins "session cwd"
set -g @tmux2k-right-plugins "docker cpu ram"
set -g @tmux2k-refresh-rate 5

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin '2kabhishek/tmux2k'
set -g @plugin 'omerxx/tmux-floax'
set -g @plugin 'tmux-plugins/tmux-cpu'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.config/tmux/plugins/tpm/tpm'
